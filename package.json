{
  "name": "cc-grok",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code wrapper that routes Anthropic Messages requests through a local Grok adapter.",
  "bin": {
    "cc-grok": "bin/cc-grok"
  },
  "scripts": {
    "check": "node --check lib/cc-grok-adapter.js && node --check lib/cc-grok-config.js && bash -n bin/cc-grok && npm test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
